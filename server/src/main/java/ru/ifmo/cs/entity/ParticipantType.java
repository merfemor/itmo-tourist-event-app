package ru.ifmo.cs.entity;

// TODO: use numbers for enums in JSON, add API for dynamically get enum values instead of hardcode on client
public enum ParticipantType {
    SINGLE,
    PAIR,
    GROUP
}
